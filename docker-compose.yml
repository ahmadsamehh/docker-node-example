version: "5.1.0"
services:
  node-app:
    build: .
    image: node-docker-image
    container_name: node-docker-container
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev