services:
  node-app:
    container_name: node-docker-production-container
    image: node-docker-production-image
    ports:
      - "5050:5050"
    environment:
      - PORT=5050
      - NODE_ENV=production
    command: npm start